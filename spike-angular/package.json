{
  "name": "spike-angular",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index",
    "browserify": "browserify ./app/app.js -o bundle.js",
    "prestart": "npm install && npm run browserify",
    "postinstall": "bower install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.8.0",
    "browserify": "^14.1.0",
    "express": "^4.14.1"
  },
  "dependencies": {
    "angular": "^1.6.2"
  }
}
